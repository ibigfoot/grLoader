This is a simple data loader for the Good Return data load.

Connects to a Force.com instance and populates Account and a custom object.

This leverages the BulkAPI from Salesforce (running user must have API permission)
